# @package _global_

defaults:
#  - _self_
  - data: cpc_mrms_data # choose datamodule with `test_dataloader()` for evaluation
  - model: wassdiff
  - metrics: default
  - callbacks: eval_callbacks
  - logger: null
  - trainer: default
  - paths: default
  - extras: default
  - hydra: default
  - _self_

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: /home/yl241/models/WassDiff-LiT/2024-09-05_20-56-22/checkpoints/epoch_740.ckpt
#ckpt_path: 'NOT_A_PATH'
# debug
data:
  dataloader_mode: specify_eval
  batch_size: 1
  data_config:
    image_size: 512
  specify_eval_targets:
    - date: '20150618'
      lon: 265
      lat: 36

model:
  num_samples: 2

callbacks:
  save_xarray_results:
    save_dir: /home/yl241/data/rainfall_plots_LiT/test_example_size_512
